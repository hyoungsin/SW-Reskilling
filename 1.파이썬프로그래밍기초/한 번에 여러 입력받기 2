'''

한 번에 여러 입력받기 2
앞에서 split()을 이용해 한 번에 여러 개의 입력을 받았습니다.

하지만 입력되는 것은 모두 문자열이기 때문에 숫자 연산을 할 수 없습니다.

숫자 연산을 하기 위해서는 입력을 int형으로 변환해야 하는데 이때 map() 함수를 사용할 수 있습니다.

a,b,c = map(int, input().split()) 
Copy
위와 같은 식으로 a, b, c에 숫자를 저장할 수 있습니다.

또한 이들을 리스트의 형태로 저장하려면 아래처럼 해주면 됩니다.

my_list = list(map(int, input().split()))
Copy
입력으로 int형 리스트가 주어질 때 리스트의 평균을 출력해 보세요.

단, 평균이 소수라면 소수점 아래는 모두 버림 합니다.

입력 예시
100 200 300

출력 예시
200

입력 예시 2

16 25 72 47 43
Copy
출력 예시 2

40
Copy

지시사항
공백을 기준으로 입력되는 숫자를 리스트에 저장하세요.

리스트의 모든 수에 대한 평균을 출력해 봅시다. 단, 소수점 아래는 모두 버림 합니다.

Tips
map()은 리스트의 요소를 지정된 함수로 처리해주는 함수입니다.
매개변수에는 map(변환 내용, 데이터) 로 map(int, input().split())은 공백을 기준으로 입력받은 것을 정수형으로 변환한다는 의미입니다.

'''

a = list(map(int,input().split()))
print(int(sum(a)/len(a)))
